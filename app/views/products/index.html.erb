<h1><%= t "products_header" %></h1>

<%= link_to "Create New", new_product_path if authenticated? %>

<ul>
    <% @products.each do |product| %>
        <li>
            <%= link_to product.name, product_path(product.id) %>
        </li>
    <% end %>
</ul>